# API URL for backend
VITE_API_URL=http://localhost:3001/api

# Enable pricing plan selection for testing the subscription flow
# Set to "true" to make plan buttons interactive (no real payment)
VITE_PRICING_TEST_MODE=true
