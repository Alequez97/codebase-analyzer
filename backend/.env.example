# Server Configuration
PORT=3001

# ========================================
# Mock Data Mode
# ========================================
# Set to "true" to use mock data instead of real LLM analysis
# Perfect for frontend development and testing without needing API keys
# Set to "false" for production use with real LLM integration
USE_MOCK_DATA=false

# ========================================
# Aider Configuration
# ========================================

# Model to use with Aider
# Examples:
#   - deepseek (DeepSeek via DeepSeek's API)
#   - sonnet (Claude 3.7 Sonnet via Anthropic's API)
#   - o3-mini (via OpenAI's API)
#   - openrouter/anthropic/claude-3.7-sonnet (via OpenRouter)
#   - openrouter/deepseek/deepseek-chat (via OpenRouter)
LLM_MODEL=sonnet

# API Keys for different providers
# Set the key for the provider you're using with Aider
DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=

# Optional: Extra arguments to pass to Aider
# Example: --no-auto-commits --dark-mode
# Aider handles token limits automatically with its repository map feature
# The default --map-tokens 1024 allows it to understand large codebases
AGENT_EXTRA_ARGS=
