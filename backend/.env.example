# Server Configuration
PORT=3001

# API Keys for different providers
# Used by both LLM API (for analysis) and Aider (for code editing)
DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=

# Optional: Extra arguments to pass to Aider (code editing tool)
# Example: --no-auto-commits --dark-mode
# Aider handles token limits automatically with its repository map feature
# The default --map-tokens 1024 allows it to understand large codebases
AIDER_STREAM_LOGS=false