/**
 * Handler for design reverse-engineer page tasks (delegated page agent)
 * Reuses the same handler logic as designGeneratePageHandler.
 */
export { designGeneratePageHandler as designReverseEngineerPageHandler } from "./generate-page.js";
